<% provide(:title, 'Lista de produtos') %>
<h1>Lista de Produtos</h1>

<%= link_to "Cadastrar produto", product_path, class: 'cadastrarproduto' %>

<%= will_paginate %>

<ul class="users">
  <% @products.each do |product| %>
    <%= render @products %>
  <% end %>
</ul>

<%= will_paginate %>