<% if @cardapio.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@cardapio.errors.count, "error") %>.
    </div>
    <ul>
    <% @cardapio.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>